dispatch:
  # Send all mobile traffic to the mobile frontend.
  - url: "rumptweets-2c7cc.appspot.com/develop"
    module: front-end-module

  # Send all work to the one static backend. 
  - url: "*/image-server/*"
    module: image-server-module